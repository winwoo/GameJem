public enum GUI_CATEGORY
{
    Common, Scene, Popup, System, UI3D
}

public static class Define
{
    public enum Scene
    {
        Unknown,
        Splash,
        Title,
        Lobby,
        Game
    }

    public enum Sound
    {
        Bgm,
        Effect,
        UI,
        Max,
    }

    public enum UIEvent
    {
        Click,
        Drag,
    }
}